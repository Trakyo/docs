---
title: "Quickstart"
description: "Get your first API call working in 2 minutes."
---

## 1. Get your API key

API keys are available from your Trakyo dashboard, or by contacting support@trakyo.io.

Your key looks like this:

```
tky_live_1c126b6be11622008e729749f963c425
```

## 2. Make your first request

<CodeGroup>
```bash curl
curl -H "Authorization: Bearer YOUR_API_KEY" \
  "https://app.trakyo.io/api/v1/links"
```

```python Python
import requests

response = requests.get(
    "https://app.trakyo.io/api/v1/links",
    headers={"Authorization": "Bearer YOUR_API_KEY"}
)
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://app.trakyo.io/api/v1/links",
  { headers: { "Authorization": "Bearer YOUR_API_KEY" } }
);
const data = await response.json();
console.log(data);
```
</CodeGroup>

## 3. Read the response

```json
{
  "links": [
    {
      "key": "abc123",
      "url": "https://example.com/page",
      "shortLink": "https://trakyo.link/abc123",
      "domainId": "00000000-0000-0000-0000-000000000000",
      "contentItemId": null,
      "clicks": 42,
      "isCustomSlug": false,
      "lastClickedAt": "2026-03-20T15:30:00Z",
      "createdAt": "2026-03-01T10:00:00Z"
    }
  ],
  "pagination": {
    "page": 1,
    "limit": 50,
    "total": 1,
    "hasMore": false
  }
}
```

## 4. Create a short link

<CodeGroup>
```bash curl
curl -X POST "https://app.trakyo.io/api/v1/links" \
  -H "Authorization: Bearer YOUR_API_KEY" \
  -H "Content-Type: application/json" \
  -d '{"url": "https://example.com/my-page"}'
```

```python Python
response = requests.post(
    "https://app.trakyo.io/api/v1/links",
    headers={
        "Authorization": "Bearer YOUR_API_KEY",
        "Content-Type": "application/json"
    },
    json={"url": "https://example.com/my-page"}
)
print(response.json())
```

```javascript JavaScript
const response = await fetch(
  "https://app.trakyo.io/api/v1/links",
  {
    method: "POST",
    headers: {
      "Authorization": "Bearer YOUR_API_KEY",
      "Content-Type": "application/json"
    },
    body: JSON.stringify({ url: "https://example.com/my-page" })
  }
);
const data = await response.json();
console.log(data);
```
</CodeGroup>

```json
{
  "key": "a1b2c3d4",
  "url": "https://example.com/my-page",
  "shortLink": "https://trakyo.link/a1b2c3d4",
  "domainId": "00000000-0000-0000-0000-000000000000",
  "contentItemId": null,
  "clicks": 0,
  "isCustomSlug": false,
  "lastClickedAt": null,
  "createdAt": "2026-03-24T12:00:00Z"
}
```

## Next steps

<CardGroup cols={2}>
  <Card title="Authentication" icon="key" href="/authentication">
    How API keys work, key format, and security.
  </Card>
  <Card title="Rate Limiting" icon="gauge" href="/concepts/rate-limiting">
    Request limits and best practices.
  </Card>
</CardGroup>
